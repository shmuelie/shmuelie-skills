# Changelog — winui3-msix

Notable changes to the **winui3-msix** skill. Repository-wide and marketplace
history is in the
[repository changelog](https://github.com/shmuelie/shmuelie-skills/blob/main/CHANGELOG.md).

The format is based on [Keep a Changelog](https://keepachangelog.com/).

## [Unreleased]

## 2026-03-21

### Added
- Initial skill: WinUI 3 data binding pitfalls (`{x:Bind}` vs `{Binding}` vs
  `[Bindable]`), MSIX packaging (`EnableMsixTooling`, manifest requirements,
  loose-file registration), WinAppSDK test project architecture, and dependency
  injection patterns.
