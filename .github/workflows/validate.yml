name: Validate marketplace

on:
  pull_request:
  push:
    branches: [main]

jobs:
  validate:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v5
      - name: Validate manifests and skills
        shell: pwsh
        run: ./scripts/Test-Marketplace.ps1
